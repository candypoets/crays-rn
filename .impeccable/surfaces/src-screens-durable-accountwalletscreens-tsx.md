---
version: 1
slug: "src-screens-durable-accountwalletscreens-tsx"
primary_target: "src/screens/durable/AccountWalletScreens.tsx"
related_targets: ["src/app/(tabs)/me.tsx","src/screens/settings/SettingsScreen.tsx","src/app/settings.tsx"]
---

Scope: Me and its identity pass; Operate mode.

Audience/job: a returning guest needs to recognize the active profile and its real signing custody, return to the current room, and reach durable night objects without confusing a Nostr identity with provider login.

Action and proof: the validated kind-0 display name, deterministic portrait, public `npub`, and custody-specific **Protected on this device** or **Connected signer** status prove which profile is in use. Expanding the single profile pass reveals the full selectable public identity and where signing stays; room and durable actions remain independent.

Direction: inherit Night Playlist and translate the profile-card placement from the earlier canonical Me studies into a compact white identity pass. The memorable moment is immediate self-recognition without displacing Current room from the first viewport.

Constraints: never expose `nsec` or NIP-46 client credentials, source self from room projections, imply human verification/global publication, or claim a remote signer key is stored on this device. Protected-store, signer, account-integrity, relay, and archive states remain distinct; names and `npub` must wrap under Dynamic Type.
