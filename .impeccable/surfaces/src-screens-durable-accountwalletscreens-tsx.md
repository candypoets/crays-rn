---
version: 1
slug: "src-screens-durable-accountwalletscreens-tsx"
primary_target: "src/screens/durable/AccountWalletScreens.tsx"
related_targets: ["src/app/(tabs)/me.tsx"]
---

Scope: Me and its local identity pass; Operate mode.

Audience/job: a returning guest needs to recognize the profile active on this device, return to the current room, and reach durable night objects without confusing a local Nostr identity with provider login.

Action and proof: the validated kind-0 display name, deterministic portrait, public `npub`, and explicit device custody prove which profile is in use. Expanding the single profile pass reveals the full selectable public identity; room and durable actions remain independent.

Direction: inherit Night Playlist and translate the profile-card placement from the earlier canonical Me studies into a compact white identity pass. The memorable moment is immediate self-recognition without displacing Current room from the first viewport.

Constraints: never expose `nsec`, source self from room projections, imply human verification/global publication, or present edit, logout, removal, provider recovery, or clipboard actions before those custody flows exist. Protected-store, account-integrity, relay, and archive states remain distinct; names and `npub` must wrap under Dynamic Type.
