---
version: 1
slug: "src-screens-durable-accountwalletscreens-tsx"
primary_target: "src/screens/durable/AccountWalletScreens.tsx"
related_targets: ["src/app/(tabs)/me.tsx","src/screens/settings/SettingsScreen.tsx","src/app/settings.tsx"]
---

Scope: Me and its identity pass; Operate mode.

Audience/job: a returning guest needs to recognize the active profile and its real signing custody, review recovery, and reach durable access/history without confusing Me with the live night.

Action and proof: the validated kind-0 display name, deterministic portrait, public `npub`, and custody-specific **Saved in Crays** or **Connected signer** status prove which profile is in use. Expanding the pass reveals the full selectable public identity; Recovery & backup, My access, and My history stay durable.

Direction: inherit Night Playlist and translate the approved final Me frame into a compact white identity pass followed by three clean ledger groups. The memorable moment is immediate self-recognition and a findable recovery path. Current room and active order belong exclusively to Tonight.

Constraints: never expose `nsec` or NIP-46 client credentials, source self from room projections, imply human verification/global publication, or claim a remote signer key is stored on this device. Protected-store, signer, account-integrity, relay, and archive states remain distinct; names and `npub` must wrap under Dynamic Type.
