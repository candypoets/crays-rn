import { Redirect, router, useLocalSearchParams } from 'expo-router'; import { useState } from 'react'; import { ensureLocalIdentity } from '@/account/account'; import { publishEvent } from '@/nostr/publish'; import { leaveTemplate } from '@/nostr/protocol'; import { relayUrlFor } from '@/rooms/relayUrl'; import { useRoomManifest } from '@/rooms/useRoomManifest'; import { SwitchRoomScreen } from '@/screens/room/LeaveAndSwitchScreens'; import { useRoomSession } from '@/session/RoomSession';
export default function SwitchRoomRoute() { const params = useLocalSearchParams<{ invite?: string; relay?: string; room?: string }>(); const { activeRoom, hydrated, leaveRoom } = useRoomSession(); const manifest = useRoomManifest(params.relay, params.room); const [switching, setSwitching] = useState(false); const [error, setError] = useState<string | null>(null); if (!hydrated) return null; if (!activeRoom) return <Redirect href={{ pathname: '/room-preview', params } as never} />; const switchRoom = async () => { if (!manifest.room || switching) return; setSwitching(true); setError(null); try { if (activeRoom.visibility === 'visible') { const identity = await ensureLocalIdentity(); await publishEvent(leaveTemplate(activeRoom.id, identity.pubkey), [relayUrlFor(activeRoom)], 'switch_leave'); } await leaveRoom('switch'); router.replace({ pathname: '/join-room', params: { invite: params.invite, relay: params.relay, room: params.room } } as never); } catch (cause) { setError(cause instanceof Error ? cause.message : 'The current room did not confirm the switch.'); } finally { setSwitching(false); } }; return <SwitchRoomScreen current={activeRoom} destination={manifest.room} error={error || manifest.error} loading={manifest.loading} onCancel={() => router.back()} onSwitch={switchRoom} switching={switching} />; }
