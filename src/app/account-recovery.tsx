import { router } from 'expo-router'; import { Ionicons } from '@expo/vector-icons'; import { Text, View } from 'react-native'; import { OnboardingShell, BackButton, PrimaryButton } from '@/components/onboarding/OnboardingPrimitives'; import { colors } from '@/theme/colors';
export default function AccountRecoveryRoute() { return <OnboardingShell testID="account-recovery-screen"><BackButton onPress={() => router.back()} /><View className="mt-10 h-16 w-16 items-center justify-center rounded-full bg-primary/15"><Ionicons color={colors.accent} name="key-outline" size={31} /></View><Text className="mt-6 text-[40px] font-black leading-[43px] text-base-content">Other ways to log in</Text><Text className="mt-4 text-lg leading-7 text-muted">Key import, remote signer, and provider recovery are not configured in this build. No local identity will be overwritten.</Text><View className="mt-auto"><PrimaryButton label="Back to login" onPress={() => router.back()} /></View></OnboardingShell>; }
