import { router, useLocalSearchParams } from 'expo-router'; import { useRoomData } from '@/rooms/RoomData'; import { OrderDetailScreen } from '@/screens/durable/NightAndOrderScreens'; import { useRoomSession } from '@/session/RoomSession';
export default function OrderRoute() { const { ref } = useLocalSearchParams<{ ref?: string }>(); const { orders } = useRoomData(); const { activeRoom } = useRoomSession(); return <OrderDetailScreen onBack={() => router.back()} order={orders.find((item) => item.orderRef === ref) || orders[0]} roomName={activeRoom?.name || 'Venue'} />; }
