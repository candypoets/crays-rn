import { router } from 'expo-router'; import { useEffect, useState } from 'react'; import { useRoomData } from '@/rooms/RoomData'; import { MeScreen } from '@/screens/durable/AccountWalletScreens'; import { useRoomSession } from '@/session/RoomSession'; import { listInviteRedemptions } from '@/invites/invites'; import { listTickets } from '@/access/tickets';
export default function MeRoute() { const data = useRoomData(); const { activeRoom } = useRoomSession(); const [hasInviteMembership, setHasInviteMembership] = useState(false); const [ticketCount, setTicketCount] = useState(0); useEffect(() => { listInviteRedemptions().then((items) => setHasInviteMembership(Boolean(items.length))); listTickets().then((items) => setTicketCount(items.length)); }, []); const activeOrder = data.orders.find((item) => !['fulfilled', 'cancelled'].includes(item.status)); const accessCount = data.entitlements.filter((item) => item.type === 'membership' || item.type === 'pass').length; return <MeScreen activeOrder={activeOrder} hasMembership={hasInviteMembership || accessCount > 0} ticketCount={ticketCount + data.entitlements.filter((item) => item.type === 'event_access').length} roomName={activeRoom?.name || activeOrder?.roomName} onMemberships={() => router.push('/memberships' as never)} onOrders={() => router.push('/orders' as never)} onProfile={() => router.push('/settings' as never)} onTickets={() => router.push('/tickets' as never)} onWallet={() => router.push('/wallet' as never)} />; }
